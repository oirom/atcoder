#include <bits/stdc++.h>
using namespace std;
typedef long int li;
typedef long long ll;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    ll n, m, x; cin >> n >> m >> x;

    // vector<vector<ll>> skill(n, vector<ll>(m,0));

    vector<ll> skill(m, 0);

    


    

    return 0;
}