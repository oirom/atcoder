#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    ll a; cin >> a;
    ld b; cin >> b;

    ll ans = (a * b);

    cout << ans << "\n";

    return 0;
}