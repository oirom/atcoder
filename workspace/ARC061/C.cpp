#include <bits/stdc++.h>
using namespace std;
typedef long int li;
typedef long long ll;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    string s; cin >> s;

    ll n = s.size(), ans = 0;

    cout << s.substr(0,1) << "\n";
    cout << s.substr(1,s.size()-1) << "\n";

    for (ll i=0; i<n; i++)
    {
        for (ll j=i+1; )
    }

    return 0;
}