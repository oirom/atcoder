#include <bits/stdc++.h>
#define ll long long
#define rep(i,n) for(ll i=0;i<n;i++)

using namespace std;

int main(){

    ll n, k, l; cin >> n >> k >> l;
    ll p[k] = q[k] = r[l] = s[l] = 0;
    rep(i,k) cin >> p[i] >> q[i];
    rep(i,l) cin >> r[i] >> s[i];

    vector<ll> dp(n+10, 1);

    


    return 0;
}